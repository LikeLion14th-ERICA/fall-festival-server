# AGENTS.md

이 문서는 저장소 전체에 적용되는 구현 에이전트 규칙이다. 세부 제품 맥락은
[`docs/PRODUCT_CONTEXT.md`](docs/PRODUCT_CONTEXT.md), 협업·PR 규칙은
[`CONTRIBUTING.md`](CONTRIBUTING.md)를 함께 읽는다.

## 1. 제품 기준

- 이 제품은 한양대학교 ERICA 가을 축제 현장에서 실제 운영되는 방문자용 웹 앱이다.
- 현재 Figma의 390×844 모바일 화면을 기준으로 모바일 우선으로 설계한다. 지원
  viewport, 브라우저, OS 조합은 출시 기준에 명시하고 해당 환경에서 검증한다.
- 일반 사용자는 로그인이나 회원가입 없이 모든 공개 기능을 이용할 수 있어야 한다.
- 고려해야 할 방문자군은 재학생, 외부 방문자, 고등학생 견학 방문자,
  영어·중국어 사용자다.
- 사용자가 가장 빨리 알아야 할 것은 `지금 진행 중인 것`, `언제 열리는지`,
  `어디에 있는지`, `부스에서 무엇을 제공하는지`, `운영 공지`다.
- 행사장 혼잡 시간대의 트래픽 급증, 느리거나 불안정한 모바일 네트워크, 운영 중
  일정·장소 변경을 정상적인 운영 조건으로 보고 설계한다.

## 2. 진실 원천과 충돌 처리

결정의 우선순위는 다음과 같다.

1. 사용자가 대화에서 명시적으로 확정한 최신 결정
2. 총학생회·학교가 제공하거나 승인한 최신 공식 자료
3. Figma 서비스 IA와 와이어프레임의 정보 구조·화면 의도
4. [`핵심 기능 API 명세서 v1`](핵심%20기능%20API%20명세서%20v1.md)의 계약

- 하위 자료가 상위 자료와 충돌하면 하위 자료를 그대로 구현하지 않는다. API 계약은
  상위 제품 결정과 일치하도록 같은 변경에서 갱신한다.
- 충돌 또는 미확정 사항은 `docs/PRODUCT_CONTEXT.md`의 결정 대기 목록에 기록하고
  제품 책임자에게 질문한다.
- Figma와 API 예시의 날짜, 아티스트, 공연, 운영 시간, URL, 영문 enum은 실제 운영
  데이터가 아니라 placeholder다.
- Figma 자료는 Figma 플러그인으로 읽는다. 사용자가 별도로 요청하지 않는 한
  브라우저 자동화나 `computer-use`로 Figma를 열람하지 않는다.

## 3. 확정된 공개 서비스 범위

- 하단 5탭: `홈 / 라인업 / 타임테이블 / 부스 & 마켓 / 지도`
- 홈: 현재 진행 중 이벤트, 공지, 챗봇, 언어 변경. FUN·스탬프가 채택되면 홈에서
  진입한다.
- 라인업: 축제 출연진 조회
- 타임테이블: 무대 일정, 현재 시각선, 현재 진행 공연 강조
- 부스 & 마켓: 부스·주점·플리마켓 목록과 상세, 메뉴·판매 품목, 지도 위치 연결
- 지도: 카테고리 필터, 구역 필터, 마커 정보. API v1은 일러스트 지도와 일반 지도
  좌표를 모두 표현하며, 실제 방식·지도 자산·사용 권한은 승인된 운영 설정으로 관리한다.
- 공지사항
- 지원 언어: 한국어, 영어, 중국어. 일본어는 후순위다.

`FAQ`, 공식 SNS 채널 구성, 라인업 검색·상세·SNS·추천곡·`내 일정에 추가`,
PWA 설치·오프라인 요건과 관리자 UI의 화면 범위는 Figma 또는 기획 제안에만 있으며
아직 공개 서비스 범위로 확정하지 않는다. API v1이 제공하는 관련 capability는 승인된
축제 회차에서 활성화되기 전까지 데이터나 UI를 공개하지 않는다. 관리자 API의
인증·권한 계약은 API v1을 따른다.

## 4. 상태 게이트

다음 기능은 승인 근거 없이 구현하거나 공개하지 않는다.

- 고등학생 환영 팝업/페이지: 총학생회 콘텐츠와 노출 조건 자료 대기
- 스탬프 투어: 참여 식별, QR, 중복 방지, 개인정보, 경품·수령 정책 협의 필요
- 분실물 안내: 하루 단위 운영 방식과 데이터 모델 보류
- FUN의 폴라로이드 및 텐텐식 술게임: 후보 기능
- 일본어: 후순위

보류 기능의 와이어프레임, 검증용 코드 또는 API 예시가 존재해도 공개 기능 승인으로
간주하지 않는다.

## 5. 학교 용어와 운영 데이터

- 사용자가 확인한 공식 한국어 풀네임은 `호공 → 호수공원`, `학정 → 학술정보관`,
  `4공 → 제4공학관`, `체대 → 예체능대학`이다.
- 사용자 노출 기본 표기는 풀네임을 사용한다. 약칭이 별도로 필요한 UX라면 제품
  책임자에게 확인한다.
- 공식 표기는 띄어쓰기 없는 `민주광장`이다.
- `호수공원 이벤트존`과 `호수공원 앞 피크닉존`은 별도 구역이다.
- 위 한국어 명칭을 근거 없이 영어·중국어로 번역하지 않는다.
- 위 용어를 추측한 영문 enum(`HOGONG_PICNIC`, `PE_PARKING`,
  `ENG4_ACADEMIC` 등)은 안정적 식별자로 사용하지 않는다.
- 학교 공간·조직·프로그램의 한·영·중 공식 표기는 사용자가 확인해 준 표기 또는
  학교가 승인한 자료만 사용한다.
- 실제 축제 날짜, 지도 이미지, 무대, 출연진, 운영 시간, 메뉴, 마커 좌표, 공지,
  외부 링크는 운영 데이터로 분리한다. 예시 값을 제품 코드에 고정하지 않는다.

## 6. 익명 접근, 보안과 개인정보

- 모든 일반 사용자 조회 화면과 챗봇 진입은 인증 없이 접근 가능해야 한다.
- 공개 경로에 로그인 리다이렉트, 회원가입, 계정 종속 즐겨찾기를 임의로 추가하지 않는다.
- 언어·필터·마지막 선택처럼 비민감한 환경 설정은 로컬 상태에 저장할 수 있지만,
  이를 사용자 계정이나 영구 개인 프로필로 확장하지 않는다.
- 관리자 기능은 별도 진입점과 서버 측 권한 검사를 갖춘 보안 경계에 둔다. access
  token은 짧은 수명의 JWT, refresh token은 회전되는 Secure·HttpOnly cookie를
  사용하고 역할, 세션 만료, 계정 복구와 긴급 권한 회수는 API v1과 운영 runbook을
  따른다.
- 모든 입력은 서버에서 검증하고 출력은 사용 맥락에 맞게 이스케이프한다. 공개 쓰기
  요청에는 속도 제한과 남용 방지를 적용하며 CORS, CSRF, 보안 헤더, 파일 업로드 정책을
  배포 환경에 맞게 명시한다.
- 비밀 값은 저장소, 클라이언트 번들, 로그에 두지 않는다. 의존성·컨테이너·배포 설정의
  취약점 검사를 CI와 출시 절차에 포함한다.
- 이름, 전화번호, 위치, 채팅 로그 등 개인정보를 수집하기 전에 목적, 최소 항목,
  보관 기간, 접근 권한, 삭제, 미성년자 처리와 동의를 확정한다.
- 로그와 분석 이벤트에는 인증 정보, 원문 개인정보, 불필요한 채팅 본문을 남기지 않는다.
- 스탬프 투어가 승인되더라도 기존 공개 기능을 로그인 뒤로 이동시키지 않는다.

## 7. 다국어

- 사용자 노출 문자열을 컴포넌트에 직접 하드코딩하지 않는다.
- UI뿐 아니라 축제명, 공간·카테고리, 무대·공연, 아티스트, 부스·메뉴, 공지,
  외부 링크 제목, 챗봇 답변을 locale별 운영 콘텐츠로 다룬다. 실제 번역 제공 범위와
  승인 주체는 제품 결정으로 확정한다.
- API v1의 locale은 `ko`, `en`, `zh`이고 `ja`는 후순위다. `zh`의 간체·번체
  선택과 한국어 기본값은 최종 결정 전 구성 가능하게 유지한다.
- 누락 번역 fallback과 실제 응답 locale을 API 계약에 명시하며, 사용자가 보고 있는
  언어와 응답 언어가 다르면 접근 가능한 방식으로 알린다.
- 단일 언어 문자가 포함된 래스터 지도만으로 다국어 지원을 충족했다고 보지 않는다.
  언어별 자산, 오버레이 텍스트, 접근 가능한 텍스트 대체 중 적합한 방식을 결정한다.
- 이미지 메뉴판만으로 끝내지 않고 검색·스크린리더·번역에 사용할 구조화 텍스트를
  제공한다.

## 8. 시간표

- 축제 기준 시간대는 API v1의 `Asia/Seoul`이며 기기의 현지 시간대만으로 현재
  공연을 계산하지 않는다.
- 현재 공연 경계 규칙, 서버 시각, 시작·종료 시각 표현, 캐시 갱신을 하나의 계약으로
  정의한다.
- 홈과 타임테이블은 같은 공연 데이터와 같은 `now` 규칙을 사용해야 한다.
- 여러 무대의 동시 공연을 지원하고, 지연·취소·시간 변경 상태와 노출 우선순위를
  데이터로 표현한다.
- 일정 변경이 게시되면 홈, 시간표, 상세, 검색, 캐시가 동일한 revision으로 전환되어야
  하며 사용자에게 마지막 갱신 시각과 변경 상태를 제공한다.

## 9. 지도

- 지도 카테고리와 구역의 원문 목록은 `docs/PRODUCT_CONTEXT.md`를 따른다.
- 일러스트 지도의 `xRatio/yRatio` 좌표는 특정 지도 자산 버전, 실제 콘텐츠 경계,
  마커 anchor에 묶고 `0..1` 범위를 검증한다. 일반 지도 좌표와 섞지 않는다.
- 구역과 카테고리는 서로 다른 분류 축이다. 필터의 선택 수와 AND/OR 규칙은 API가
  반환하는 현재 지도 설정을 따르고 클라이언트에 별도 기본값을 두지 않는다.
- 마커 선택, 바텀시트, 부스 상세의 `지도에서 위치 보기`는 동일한 안정적 marker ID를
  사용한다.
- 지도만으로 정보를 숨기지 말고 접근 가능한 목록·텍스트 경로를 유지한다.
- `의무실`은 지도 카테고리에 포함한다.
- `매표소`, `티켓부스`, `티켓 수령 부스`는 같은 물리적 장소다. 별도 좌표를 중복
  생성하지 않으며 대표 UI 명칭과 구역·마커 분류는 결정 후 고정한다.
- 지도 카테고리의 `공연장`은 공연장 게이트를 포함한 공연장 전체를 뜻한다. 게이트를
  독립 최상위 카테고리로 다시 분리하지 않는다.
- 지도 자산을 교체할 때는 새 map version을 발행하고 좌표 검증을 거친 뒤 원자적으로
  전환한다. 자산과 마커 revision이 다른 상태를 공개하지 않는다.

## 10. 저장소 구조와 API

- `test/frontend`에는 Next.js 기반 PWA·실기기 검증 UI가 있고, 선언된 검증 명령은
  `npm run check`다.
- `test/backend`에는 Java 21·Spring Boot·PostgreSQL 기반 검증 API가 있다. Maven
  변경은 최소 `mvn verify`로 검증하며 통합 환경이 필요한 검사는 별도로 기록한다.
- `test/docker-compose.yml`과 `test/Caddyfile`은 동일 HTTPS origin의 실행 구성을,
  `test/README.md`와 `test/docs/FREE_SERVER_DEPLOYMENT.md`는 해당 검증 서비스의
  실행·운영 계약을 설명한다.
- `test/`의 스탬프·Web Push 코드는 실기기 검증용이다. 이 코드의 존재는 스탬프,
  PWA, Push를 공개 서비스 범위로 승인하거나 전체 제품의 아키텍처로 확정한 것이 아니다.
- API v1 문서를 공개 앱과 관리자 도구가 공유하는 계약으로 사용한다. 구현 기술 스택은
  계약과 분리하며 승인된 아키텍처 결정 기록을 따른다.
- API 요청·응답·enum·오류·페이지네이션을 변경하는 PR은 같은 PR에서 명세와 계약
  테스트를 갱신한다.
- v1의 호환성을 유지한다. 기존 클라이언트를 깨는 필드 제거·의미 변경·enum 재해석은
  새 API 버전과 마이그레이션 계획 없이 배포하지 않는다.
- 목록/상세 응답, 오류, pagination, locale fallback, 시간 표현을 OpenAPI 또는 동등한
  기계 판독 계약으로 검증한다. 오류 응답은 안전한 메시지와 추적 가능한 request ID를
  포함하고 내부 정보는 노출하지 않는다.
- 모든 운영 콘텐츠는 축제 회차와 revision에 귀속한다. 지도 위치는 map version에도
  귀속하며, 표시 이름과 안정적 ID를 분리한다.
- 날짜·시각은 timezone을 잃지 않는 형식으로 교환하고, 정렬과 pagination은 동일 요청의
  반복에서도 안정적이어야 한다.
- 학교 약칭이나 번역 문구에서 식별자를 만들지 않는다.

## 11. 콘텐츠 게시, 캐시와 복구

- 운영 콘텐츠는 최소 `draft / scheduled / published / archived` 생명주기와 게시자,
  게시·수정 시각, revision, 감사 이력을 갖는다. 공개 API는 게시 승인된 revision만
  반환한다.
- 관리자 변경은 미리보기와 유효성 검사를 거친다. 시간표·지도처럼 여러 객체가 함께
  바뀌는 변경은 부분 게시되지 않도록 하나의 게시 단위로 취급한다.
- 긴급 공지는 예약 콘텐츠보다 우선하며 즉시 게시·회수할 수 있어야 한다. 게시 또는
  회수 후 관련 CDN, API, 애플리케이션 캐시를 명시적으로 무효화한다.
- 캐시 키에는 축제 회차, 콘텐츠 revision, locale, map version 등 응답을 바꾸는 요소를
  포함한다. 긴급 공지와 현재 공연에는 짧은 신선도 정책을, 이미지와 버전 고정 자산에는
  장기 캐시를 적용한다.
- 네트워크 또는 API 장애 시 검증된 마지막 데이터로 제한된 기능을 제공할 수 있지만,
  오래된 데이터임을 표시하고 긴급 공지를 무기한 캐시하지 않는다.
- 운영 데이터와 설정은 정기 백업하며 복구 시점·복구 시간 목표를 출시 전에 정한다.
  백업 존재 여부가 아니라 실제 복원 훈련으로 검증한다.
- 배포·스키마 변경·대량 콘텐츠 게시에는 롤백 절차와 담당자를 두고, 이전의 일관된
  revision으로 복구할 수 있어야 한다.

## 12. 접근성, 성능과 관측성

- 공개 사용자 화면은 WCAG 2.2 AA를 목표로 구현하고 자동 검사와 키보드·스크린리더
  수동 검증을 함께 수행한다.
- 의미 구조, 포커스 순서와 표시, 충분한 대비, 44×44 CSS px 수준의 터치 영역,
  확대·reflow, 동작 감소 설정을 지원한다. 색상·지도·이미지만으로 정보를 전달하지 않는다.
- 실제 모바일 환경의 75번째 백분위에서 Core Web Vitals 권장 기준인
  `LCP ≤ 2.5s`, `INP ≤ 200ms`, `CLS ≤ 0.1`을 출시 성능 목표로 삼는다.
- 초기 화면의 필수 정보와 긴급 공지를 우선 전송하고 이미지 크기·형식·해상도를 최적화한다.
  저속 네트워크와 행사 피크 트래픽을 포함한 부하·복원력 테스트를 출시 전에 수행한다.
- 구조화 로그, 지표, 분산 추적 또는 동등한 요청 추적을 갖추고 API 오류율·지연,
  캐시 상태, 게시 실패, 챗봇 실패, 클라이언트 오류를 대시보드와 경보로 연결한다.
- 서비스 수준 목표, 경보 임계치, 온콜 연락망, 장애 대응·복구 runbook을 운영 전에
  확정한다. 관측 데이터는 locale·기능·revision별 문제를 찾을 수 있어야 하며 개인정보는
  포함하지 않는다.
- staging에서 운영과 동일한 배포·마이그레이션·캐시 무효화 경로를 검증한다. 배포 후
  health check와 핵심 사용자 흐름의 smoke test가 실패하면 롤백한다.

## 13. 작업 절차

1. 변경 전 이 문서, `docs/PRODUCT_CONTEXT.md`, 관련 Figma node, API v1과 변경 대상
   하위 디렉터리의 운영 문서를 읽는다.
2. 요구사항을 `확정 / 자료 대기 / 협의·보류 / 후순위 / 제안` 중 하나로 분류한다.
3. 학교 용어 또는 상위·하위 자료 충돌을 발견하면 추측하지 말고 질문한다.
4. 한 PR에는 하나의 목적 또는 하나의 기능 도메인만 담는다.
5. 새 추상화를 만들기 전에 기존 컴포넌트·토큰·패턴을 재사용한다.
6. 기능 테스트뿐 아니라 계약, 접근성, 보안, 성능, 관측성, 캐시 무효화와 복구 영향을
   변경 위험에 비례해 검증한다.
7. 관련 테스트, 타입 검사, 린트, 빌드를 실행하고 결과와 미실행 사유를 PR에 기록한다.
8. 저장소에 선언된 명령만 실행한다. toolchain이나 배포 절차가 바뀌면 실제 명령과
   운영 runbook을 같은 변경에서 갱신한다.
